export const API_HOST = 'https://pokeapi.co/api/v2' as const;

export const FORMAT_HOUR = 'HH:mm:ss' as const;
export const FORMAT_DATE = `YYYY-MM-DD ${FORMAT_HOUR}` as const;
export const FORMAT_DATE_VIEW = `DD-MM-YYYY ${FORMAT_HOUR}` as const;
